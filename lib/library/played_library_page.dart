import 'package:flutter/material.dart';

class LibraryPlayed extends StatelessWidget {
  const LibraryPlayed({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text("played");
  }
}
