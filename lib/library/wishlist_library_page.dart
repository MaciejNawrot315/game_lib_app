import 'package:flutter/material.dart';

class LibraryWishlist extends StatelessWidget {
  const LibraryWishlist({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text("wishlist");
  }
}
